image: php:7.0.27

pipelines:
  default:
    - step:
        script:
          - apt-get update || true && apt-get install -y zip
          - apt-get -qq install git-ftp
          - git ftp push --user $SFTP_username --passwd $SFTP_password --verbose sftp://FTP_HOST/var/www/html